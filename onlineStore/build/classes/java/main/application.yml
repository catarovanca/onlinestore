server:
  port: 9000
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/onlineStore
    username: psqlRoot
    password: psqlPassword
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true